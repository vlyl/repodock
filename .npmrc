# Use a stable, hoisted layout that WXT and Vite expect.
# WXT's build tooling resolves plugins from the project root, so we keep a
# shamefully-hoisted node_modules to avoid phantom-dependency resolution issues.
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
